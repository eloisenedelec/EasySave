<UserControl x:Class="EasySave.Views.SettingsOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:EasySave.UI">

    <UserControl.Resources>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#415A77"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1B263B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#778DA9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F1F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RemoveBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0AABB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FDECEA"/>
                                <Setter Property="Foreground" Value="#C0392B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#415A77"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D7E2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#FFFFFF"
            CornerRadius="12"
            Padding="32,28"
            Width="440"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.18" Color="#000000"/>
        </Border.Effect>

        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="560">
        <StackPanel>

            <TextBlock Text="{Binding [settings_title], Source={x:Static ui:LanguageManager.Instance}}"
                       FontSize="18" FontWeight="Bold"
                       Foreground="#1B263B"
                       Margin="0,0,0,24"/>

            <!-- langues -->
            <TextBlock Text="{Binding [settings_language], Source={x:Static ui:LanguageManager.Instance}}" Style="{StaticResource SectionLabel}"/>
            <ListBox ItemsSource="{Binding Languages}"
                     SelectedItem="{Binding SelectedLanguage}"
                     Background="Transparent"
                     BorderThickness="0"
                     Margin="0,0,0,20">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Label}" FontSize="13" Padding="4,6"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EEF2F7"/>
                                <Setter Property="Foreground" Value="#1B263B"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F7FA"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- format des logs -->
            <TextBlock Text="{Binding [settings_logformat], Source={x:Static ui:LanguageManager.Instance}}" Style="{StaticResource SectionLabel}"/>
            <ComboBox ItemsSource="{Binding LogFormats}"
                      SelectedItem="{Binding SelectedLogFormat}"
                      Padding="8,6"
                      FontSize="13"
                      Margin="0,0,0,20"/>

            <!-- logiciels métiers à surv -->
            <TextBlock Text="{Binding [settings_processes], Source={x:Static ui:LanguageManager.Instance}}" Style="{StaticResource SectionLabel}"/>
            <ItemsControl ItemsSource="{Binding Processes}" Margin="0,0,0,8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9FAFB" CornerRadius="6" Padding="10,6" Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" FontSize="13" Foreground="#1B263B"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="✕"
                                        Style="{StaticResource RemoveBtn}"
                                        Command="{Binding DataContext.RemoveProcessCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding NewProcessName, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputBox}"
                         Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="{Binding [btn_add], Source={x:Static ui:LanguageManager.Instance}}"
                        Style="{StaticResource PrimaryBtn}"
                        Command="{Binding AddProcessCommand}"/>
            </Grid>

            <!-- extensions à chiffrer -->
            <TextBlock Text="{Binding [settings_extensions], Source={x:Static ui:LanguageManager.Instance}}" Style="{StaticResource SectionLabel}"/>
            <ItemsControl ItemsSource="{Binding Extensions}" Margin="0,0,0,8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9FAFB" CornerRadius="6" Padding="10,6" Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding}" FontSize="13" Foreground="#1B263B"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="✕"
                                        Style="{StaticResource RemoveBtn}"
                                        Command="{Binding DataContext.RemoveExtensionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Margin="0,0,0,28">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding NewExtension, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputBox}"
                         Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="{Binding [btn_add], Source={x:Static ui:LanguageManager.Instance}}"
                        Style="{StaticResource PrimaryBtn}"
                        Command="{Binding AddExtensionCommand}"/>
            </Grid>

            <!-- extensions prioritaires -->
            <TextBlock Text="{Binding [settings_priority_ext], Source={x:Static ui:LanguageManager.Instance}}" Style="{StaticResource SectionLabel}"/>
            <ItemsControl ItemsSource="{Binding PriorityExtensions}" Margin="0,0,0,8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFF8E1" CornerRadius="6" Padding="10,6" Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding}" FontSize="13" Foreground="#1B263B" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="✕"
                                        Style="{StaticResource RemoveBtn}"
                                        Command="{Binding DataContext.RemovePriorityExtensionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding NewPriorityExtension, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputBox}"
                         Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="{Binding [btn_add], Source={x:Static ui:LanguageManager.Instance}}"
                        Style="{StaticResource PrimaryBtn}"
                        Command="{Binding AddPriorityExtensionCommand}"/>
            </Grid>

            <!-- taille limite gros fichiers -->
            <TextBlock Text="{Binding [settings_large_file_limit], Source={x:Static ui:LanguageManager.Instance}}" Style="{StaticResource SectionLabel}"/>
            <Grid Margin="0,0,0,28">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding LargeFileSizeKb, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputBox}"
                         Margin="0,0,8,0"/>
                <TextBlock Grid.Column="1" Text="Ko" FontSize="13" Foreground="#778DA9" VerticalAlignment="Center"/>
            </Grid>

            <!-- ouvrir les logs, cancel ou save -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{Binding [btn_open_logs], Source={x:Static ui:LanguageManager.Instance}}"
                        Style="{StaticResource GhostBtn}"
                        Command="{Binding OpenLogsCommand}"
                        Margin="0,0,8,0"/>
                <Button Content="{Binding [btn_cancel], Source={x:Static ui:LanguageManager.Instance}}"
                        Style="{StaticResource GhostBtn}"
                        Command="{Binding CloseCommand}"
                        Margin="0,0,8,0"/>
                <Button Content="{Binding [btn_save], Source={x:Static ui:LanguageManager.Instance}}"
                        Style="{StaticResource PrimaryBtn}"
                        Command="{Binding SaveCommand}"/>
            </StackPanel>

        </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
